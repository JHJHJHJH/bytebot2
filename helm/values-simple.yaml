# Simple Bytebot configuration example.
# LLM provider API keys are added after deployment from the UI at /models.

bytebot-agent:
  # Keep /models API keys across pod restarts.
  persistence:
    enabled: true

# That's it! The defaults handle everything else.
# 
# For more options, see the full values.yaml file.
